---
const { href, id, target, color, iconClass } = Astro.props;
---

<a
  href={href}
  id={id}
  target={target}
  rel="prefetch"
  class:list={[
    "text-f-sm hover:ease flex place-content-center items-center rounded-full pl-4 pr-6 py-2 fw600 text-white shadow-md hover:transition-all hover:duration-150 focus:outline-none focus:ring-2 focus:ring-slate-500/50 focus:ring-offset-2 focus:ring-offset-transparent dark:text-dark-700 w-5/6 sm:w-auto",
    color,
  ]}
  tabindex="0"
>
  <span class={iconClass}></span>
  <slot />
</a>
