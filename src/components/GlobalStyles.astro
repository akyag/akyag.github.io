<style is:global>
  /* @link https://utopia.fyi/type/calculator?c=320,19,1.2,1536,20,1.414,5,2,&s=0.75|0.5|0.25,1.5|2|3|4|6,s-l */

  /* :root {
  --step--2: clamp(0.63rem, calc(0.88rem + -0.26vw), 0.82rem);
  --step--1: clamp(0.88rem, calc(1.02rem + -0.14vw), 0.99rem);
  --step-0: clamp(1.19rem, calc(1.17rem + 0.08vw), 1.25rem);
  --step-1: clamp(1.43rem, calc(1.33rem + 0.45vw), 1.77rem);
  --step-2: clamp(1.71rem, calc(1.5rem + 1.04vw), 2.5rem);
  --step-3: clamp(2.05rem, calc(1.66rem + 1.95vw), 3.53rem);
  --step-4: clamp(2.46rem, calc(1.8rem + 3.33vw), 5rem);
  --step-5: clamp(2.96rem, calc(1.87rem + 5.41vw), 7.07rem);
} */

  /* .text-f-xs {
  font-size: var(--step--2);
}

.text-f-sm {
  font-size: var(--step--1);
}

.text-f-base {
  font-size: var(--step-0);
}

.text-f-lg {
  font-size: var(--step-1);
}

.text-f-xl {
  font-size: var(--step-2);
}

.text-f-2xl {
  font-size: var(--step-3);
}

.text-f-3xl {
  font-size: var(--step-4);
}

.text-f-4xl {
  font-size: var(--step-5);
} */

  :root {
    --lightCoral: #f57e7e;
    --lightRose: #fbcbcb;
  }

  .font-body {
    font-family: "Atkinson Hyperlegible", sans-serif;
  }

  .font-display {
    font-family: "Mulish", sans-serif;
  }

  /* .custom-gradient { */
  /*   mask-image: linear-gradient(to bottom, transparent, black); */
  /*   -webkit-mask-image: linear-gradient(to bottom, transparent, black); */
  /* } */

  @keyframes leftslide {
    0% {
      transform: translate(-30px);
      opacity: 0;
    }
    to {
      transform: translate(0);
      opacity: 1;
    }
  }
  .animate-leftslide {
    animation: leftslide ease 0.5s backwards;
  }
  @keyframes rightslide {
    0% {
      transform: translate(30px);
      opacity: 0;
    }
    to {
      transform: translate(0);
      opacity: 1;
    }
  }
  .animate-rightslide {
    animation: rightslide ease 0.5s backwards;
  }
  @keyframes lift {
    0% {
      transform: translateY(30px);
      opacity: 0;
    }
    to {
      transform: translateY(0);
      opacity: 1;
    }
  }
  .animate-lift {
    animation: lift ease 0.5s backwards;
  }
  @keyframes pop {
    0% {
      transform: scale(0.8);
      opacity: 0;
    }
    to {
      transform: scale(1);
      opacity: 1;
    }
  }
  .animate-pop {
    animation: pop ease 0.5s backwards;
  }
  @keyframes drop {
    0% {
      transform: translateY(-30px);
      opacity: 0;
    }
    to {
      transform: translateY(0);
      opacity: 1;
    }
  }
  .animate-drop {
    animation: drop ease 0.5s backwards;
  }
</style>
