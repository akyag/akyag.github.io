---
export interface Props extends astroHTML.JSX.AnchorHTMLAttributes {}

const { href, ...props } = Astro.props as Props;

const isActive = href === Astro.url.pathname;
---

<a
  href={href}
  rel="prefetch"
  class:list={[
    "text-f-base font-display text-dark-500 hover:text-geraldine-600 hover:transition-all hover:duration-200 hover:ease-in-out focus:border-b-4 focus:border-geraldine-400 focus:outline-none dark:text-white dark:hover:text-geraldine-400",
    {
      "border-b-4 border-geraldine-500": isActive,
    },
  ]}
  tabindex="0"
  {...props}
>
  <slot />
</a>
