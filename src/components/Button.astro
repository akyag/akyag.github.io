---
const { href, id, target, color } = Astro.props;
---

<a
  href={href}
  id={id}
  rel="prefetch"
  target={target}
  class:list={[
    "text-f-sm hover:ease flex content-center items-center rounded-md px-6 py-3 font-semibold text-white shadow-md hover:transition-all hover:duration-150 focus:outline-none focus:ring-2 focus:ring-slate-500/50 focus:ring-offset-2 focus:ring-offset-transparent dark:text-dark-700",
    color,
  ]}
  tabindex="0"
>
  <slot />
</a>
